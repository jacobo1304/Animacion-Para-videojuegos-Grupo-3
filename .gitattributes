######################################################################
# Unity / GameJam .gitattributes (seguro y “universal”)
#
# Objetivo:
# - Nunca corromper binarios (audio/imagenes/modelos) por EOL.
# - Normalizar texto importante a LF.
# - Habilitar unityyamlmerge para escenas/prefabs/assets YAML.
# - Usar Git LFS solo para archivos realmente pesados.
######################################################################

# Por defecto NO tratar archivos como texto (evita que Git intente EOL en binarios raros)
* -text

# ===============================
# Macros
# ===============================
[attr]lfs         filter=lfs diff=lfs merge=lfs -text
[attr]unity-yaml  merge=unityyamlmerge text eol=lf

# ===============================
# Texto (normalizar a LF)
# ===============================
*.cs              text diff=csharp eol=lf
*.cginc           text eol=lf
*.compute         text eol=lf
*.hlsl            text eol=lf
*.raytrace        text eol=lf
*.shader          text eol=lf
*.txt             text eol=lf
*.md              text eol=lf
*.json            text eol=lf
*.yml             text eol=lf
*.yaml            text eol=lf
*.xml             text eol=lf
*.csv             text eol=lf
*.tsv             text eol=lf

# Unity JSON-like (texto)
*.asmdef          text eol=lf
*.asmref          text eol=lf
*.inputactions    text eol=lf
*.shadergraph     text eol=lf
*.shadersubgraph  text eol=lf

# ===============================
# Unity YAML (smart merge)
# ===============================
*.meta                 unity-yaml
*.unity                unity-yaml
*.prefab               unity-yaml
*.asset                unity-yaml
*.anim                 unity-yaml
*.controller           unity-yaml
*.overrideController   unity-yaml
*.mat                  unity-yaml
*.mixer                unity-yaml
*.preset               unity-yaml
*.spriteatlas          unity-yaml

# ===============================
# Binarios (NO tocar EOL)
# ===============================
# Audio
*.wav            binary -text
*.mp3            binary -text
*.ogg            binary -text
*.aiff           binary -text
*.flac           binary -text

# Imágenes
*.png            binary -text
*.jpg            binary -text
*.jpeg           binary -text
*.tga            binary -text
*.gif            binary -text
*.bmp            binary -text
*.exr            binary -text
*.psd            binary -text
*.psb            binary -text

# Fuentes
*.ttf            binary -text
*.otf            binary -text

# Plugins / binarios
*.dll            binary -text
*.so             binary -text
*.dylib          binary -text
*.exe            binary -text

# ===============================
# Git LFS (solo lo pesado de verdad)
# Nota: No usamos LFS para .png/.wav/.mp3 para que cualquiera clone sin git-lfs.
# ===============================
*.fbx            lfs
*.blend          lfs
*.obj            lfs
*.mp4            lfs
*.mov            lfs
*.avi            lfs
*.mkv            lfs
*.unitypackage   lfs
*.zip            lfs
*.7z             lfs
*.rar            lfs

